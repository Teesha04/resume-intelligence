# --- Core ---
pydantic>=2.6
pydantic-settings>=2.2

# --- Ingestion ---
pdfplumber>=0.11.0   # primary PDF text + layout extraction
pypdf>=4.2.0         # fallback PDF extractor / page count
python-docx>=1.1.0   # optional DOCX support (bonus)

# --- Network / enrichment ---
httpx>=0.27.0        # GitHub REST + provider HTTP with timeouts
tenacity>=8.3.0      # bounded retries for transient failures

# --- LLM adapter (optional at runtime) ---
google-genai>=1.0.0  # Gemini structured output

# --- API (bonus) ---
fastapi>=0.111.0
uvicorn>=0.30.0

# --- Tests ---
pytest>=8.2.0
